import 'package:flutter/material.dart';

class Achievementtab extends StatelessWidget {
  const Achievementtab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Achievment'),
      ),
    );
  }
}
